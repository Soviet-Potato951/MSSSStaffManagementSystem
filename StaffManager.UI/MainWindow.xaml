<Window x:Class="StaffManager.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:services="clr-namespace:StaffManager.UI.Services"
        xmlns:local="clr-namespace:StaffManager.UI"
        mc:Ignorable="d"
        Title="Staff Manager"
        Width="900" Height="500"
        Loaded="Window_Loaded">

    <Window.Resources>
        <services:EnumToBooleanConverter x:Key="EnumToBooleanConverter"/>
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Key="G" Modifiers="Alt" Command="{Binding DataContext.OpenAdminCommand, RelativeSource={RelativeSource AncestorType=Window}}" />
    </Window.InputBindings>

    <Window.CommandBindings>
        <CommandBinding Command="{x:Static local:AppCommands.FocusName}" Executed="FocusName_Executed"/>
        <CommandBinding Command="{x:Static local:AppCommands.FocusId}" Executed="FocusId_Executed"/>
        <CommandBinding Command="{x:Static local:AppCommands.PopulateFromSelection}" Executed="PopulateFromSelection_Executed"/>
    </Window.CommandBindings>


    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Filter inputs -->
        <Border Grid.Row="0" Padding="10" CornerRadius="6" Background="White" BorderBrush="#FFCCCCCC" BorderThickness="1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Label Content="Filter Name:"/>
                <TextBox Name="NameFilterTextBox" Width="180" Text="{Binding FilterName, UpdateSourceTrigger=PropertyChanged}"/>
                <Label Content="Filter ID:"/>
                <TextBox Name="IdFilterTextBox" Width="180" Text="{Binding FilterId, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
        </Border>

        <!-- Main content: 2 listboxes -->
        <Border Grid.Row="1" Padding="12" Margin="0,12"
                Background="White" BorderBrush="#FFCCCCCC" BorderThickness="1" CornerRadius="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <ListBox x:Name="lstUnsorted"
                         Grid.Column="0"
                         ItemsSource="{Binding UnsortedEntries}"
                         Focusable="False"/>

                <Rectangle Grid.Column="1" Width="12" Fill="Transparent"/>

                <ListBox x:Name="lstFiltered" 
                         Grid.Column="2" 
                         ItemsSource="{Binding FilteredEntries}" 
                         SelectedItem="{Binding SelectedStaff, Mode=TwoWay}" 
                         Focusable="True"/>
            </Grid>
        </Border>
        <!-- Controls -->
        <Border Grid.Row="2" Padding="10" CornerRadius="6"
                Background="White" BorderBrush="#FFCCCCCC" BorderThickness="1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Focus Name Alt+N" Width="120" Command="{x:Static local:AppCommands.FocusName}" Margin="4,0"/>
                <Button Content="Focus Id Alt+I" Width="120" Command="{x:Static local:AppCommands.FocusId}" Margin="4,0" />
                <Button Content="Open Admin Alt+G" Width="120" Command="{Binding OpenAdminCommand}" Margin="4,0"/>

                <TextBlock Text="Storage Mode:" VerticalAlignment="Center" FontWeight="Bold"/>
                <RadioButton Content="Dictionary"
                             GroupName="StoreMode"
                             IsChecked="{Binding CurrentStoreMode, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Hash}"
                             Margin="4,0"/>
                <RadioButton Content="SortedDictionary"
                             GroupName="StoreMode"
                             IsChecked="{Binding CurrentStoreMode,Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Tree}"
                             Margin="4,0"/>
            </StackPanel>
        </Border>

        <!-- Status bar -->
        <StatusBar Grid.Row="3" Margin="0,8,0,0">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}" />
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="{Binding CurrentStoreMode}" FontWeight="Bold"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>